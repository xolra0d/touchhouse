use crate::sql::sql_parser::LogicalPlan;

impl LogicalPlan {
    // pub fn remove_dead_query() -> Self {
    //     todo!("if limit is 0 or offset = table rows or no proj, then just LogicalPlan::Skip")
    // }
}
